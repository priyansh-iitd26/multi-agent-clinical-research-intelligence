[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "multi_agent_clinical_trial_intelligence"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]